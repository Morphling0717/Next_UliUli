# Next_UliUli：共享风铃 0.8.3 升级

验证日期：2026-09-23；Node v20.20.2、npm 10.9.8、Windows x64。

本轮固定安装同一份 windchime-embed-0.8.3.tgz，SHA256：c1207a59e8a3d5f9fe9e4e9a6c1895250569f2985add9b08c9737d8170a8f865。共享源码来源：6b53289c6f0a513d8789d099dc882c13119142b5。原 0.8.2 压缩包与旧证据保留。

在 93e243e6d8463875b3f28146c53bc9a9cc077686 的独立 Git 工作树中只升级 package.json、package-lock.json 和新 vendor 包；精确文件摘要在 report.json。独立 npm ci、生产构建、原有 HTTP 回归，以及 fresh / legacy / current 三种数据库各两次迁移全部通过；没有复用前次结果。迁移测试保留原信、状态、话题、授权范围与哈希、到期及撤销关系。两站管理密钥、展示密钥及 Cookie 双向越权均被拒绝。

HTTP 回归包括新信默认不播、批准不自动播放、手动上屏/隐藏、撤销/改稿撤下、展示握手空白、只读及话题隔离、队列移除/重排不绕回播出，以及投稿、筛选、收藏、归档恢复、批量操作、拉黑/解除和设置。Service Worker 的缓存排除用 VM 验证，不代表浏览器离线验收。

原工作区 tsconfig.json 未写入，SHA256 仍为 dd75b2ef28fc05eba3dc1cb6feb2eebfb249e976a891fe9255bdb8dd73492d67。临时站点仅监听 127.0.0.1:3161/3162，结束时均已关闭。仅使用临时数据库、媒体目录和合成凭据，未读取生产环境或数据库。

两次构建仍有 NFT 范围追踪及 Edge 静态生成警告，详情见日志。此记录不代表真实浏览器、图片像素、OBS/直播姬采集、桌面安装/卸载或生产部署已验收。
