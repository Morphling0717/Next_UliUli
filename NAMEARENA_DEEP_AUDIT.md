# 名字大乱斗全系统深度审查报告

> 审查日期：2026-07-22 至 2026-07-23
> 审查范围：战斗引擎、统一状态、14 名特殊角色、水人规则、NPC、召唤物、组队、日志、播放、动画、经典/新舞台、移动端和确定性性能问题。
> 保护措施：实施前完整差异快照位于 `/tmp/next-uliuli-pre-deep-audit-20260722.9u2Xtn`。本轮没有修改数据库，没有推送 GitHub，也没有部署服务器。

## 最终结论

- 已确认的确定性 Bug、日志因果错误、播放不同步和界面布局问题均已修复，并补充了对应回归或固定种子复现。
- 转阶段现在只有一个权威入口和一个权威视觉事件；1→2、2→3 不再重复播放，同阶段换职业不会再冒充转阶段。
- 状态结算、伤害链、目标替换、分摊、转移、护盾、锁血、复活、死亡与日志已按同一因果顺序工作。
- 1,000 场视觉审计处理 4,492,817 个结构化事件，未发现重复阶段边、同阶段转职、无关日志携带动画或视觉事件重复 ID。
- 11,500 场分层覆盖没有运行错误、状态不变量错误或日志扫描错误；2v2 至 5v5 的任意角色关系覆盖均达到要求。
- 最终版 15,000 场无水 FFA 没有超时、运行错误、状态不变量错误或日志错误；同一批种子的超时从修复前 109 场降为 0。
- 20 份完整战报共 35,858 条编号事件已经逐条人工审阅。审阅中发现的问题均回到代码根因修复，而不是只改文案。
- 本轮没有按胜率调整角色数值。达到回合上限的对局单独记录为高续航长局，不用削弱角色来掩盖。

## 权威形态与动画

### 原问题

鸮和萌月沫沫的一阶段转二阶段会被拆成“职业变化”和“阶段变化”两个事件。旧界面又会比较任意日志前后的角色快照来猜测变身，因此一个真实阶段边可能出现多次全屏动画，普通日志也可能错误继承变身提示。初始 100 场样本中发现了 200 个同阶段转职提示，以及 170 次同角色同一行动多次变身提示。

### 修复

- 新增 `commitFormTransition`，先原子提交职业、阶段、属性、生命、形态字段和专属副作用，再生成唯一日志与视觉事件。
- 真实 1→2、2→3 使用 `transformation`；同阶段换形态、复活换形态和重新部署使用短动画 `form_shift`。
- 每个视觉提示拥有稳定 `visualCueId`，并携带结构化 `actorId`、`targetIds`、`actionId`、`from`、`to` 和 `cause`。
- 删除通过玩家可见日志文字或前后快照推断转阶段、施法者、目标和小汀技能特效的运行时逻辑。
- 增加 TypeScript AST 架构检查：初始化白名单之外，运行中职业、阶段和变身字段不得绕过统一入口直接改写。

### 验证

- 1,000 场固定种子视觉审计：`transformation` 15,539 次、`form_shift` 1,005 次。
- 同阶段 `transformation`：0。
- 同一角色同一阶段边重复提示：0。
- 提示挂到无关日志：0。
- 重复或缺失 `visualCueId`：0。

## 播放与视觉管线

| 问题 | 根因 | 修复与结果 |
| --- | --- | --- |
| 上一行动的动作在下一行动重播 | 视觉去重状态属于容易重挂载的舞台组件，且早期版本会淘汰较旧的已播 ID | 页面级账本按 `battleRunId + visualCueId` 在整局内永久认领；即使超过 4,096 个提示也不会遗忘，正式新开局才整体重置 |
| 多个全屏动画覆盖或乱序 | 单一动画槽直接被新事件替换 | 改为互斥有序队列，同一事件只入队一次 |
| 切换战斗、隐藏页面后旧特效继续出现 | 延迟任务、RAF 和角色位移没有统一取消 | 重置、换局、隐藏、卸载统一取消计时器、帧任务、位移和延迟后续 |
| 小汀自爆提前回原位 | 位移 Promise 与爆炸/吸血后续不共用取消和时序 | 固定为贴近→蓄力→自爆→回位→吸血；取消时不再补播吸血弹窗 |
| 日志与伤害帧错开一个行动 | 日志提交和舞台快照分别推进 | 统一播放提交模型，先提交当前事件对应的权威快照，再允许进入下一日志 |
| x2/x3 下动画没结束日志已前进 | 日志停留时间和动画时长各自维护 | 使用共享视觉时长目录，以最长动画时长加缓冲作为最短停留时间 |
| 普攻、技能、必杀和普通召唤物混用动画 | 通过名称和文本猜分类 | 行动显式声明 `basic/skill/finisher`；普通召唤物即使技能名相似也不能冒充必杀 |

## 战斗规则与状态

### 已修复的规则问题

- 多种控制并存时，清除其中一种曾会把全部 `blocked/confused/embarrassed` 标记一起清掉。现在每次处理后都从权威状态集合重新计算行动限制。
- 显式组队中的水人曾被无条件改到内部 `WATER_TEAM`，会攻击同为 `@A` 的队友。现在显式队名优先，无队名 FFA 才使用水人阵营；好大儿、水人大女儿和召唤物递归继承真实队伍。
- 幽灵鲨濒死锁血、蛐蛐自刎锁血曾晚于“没有承受实际伤害”，造成结果先于原因。锁血现已进入统一减伤/命运阶段。
- 柚子结果摘要曾把召唤物的濒死锁血统称为阶段锁血。伤害结果现在携带精确锁血名称，区分阶段锁血、自刎锁血、濒死锁血、不甘倒下和悲愿不倒。
- 复活重建曾在日志中显示为水人的【绝对驱散】。绝对驱散现在只保留给水人；普通复活使用独立清理原因。
- 【武神王座】的控制免疫、法术抵挡和等待反击没有稳定主题组，局部耗尽会误报整套状态结束。三个机械现共用主题组，部分结束时会列出仍生效机械。
- 刺猬人的净化决策曾把“复活时清理的身份清单”误当成“当前可驱散异常清单”。不可驱散的【镜界标记】因此会强制三阶段刺猬人每次行动都使用【奇迹炼成装甲】，而技能又无法清除标记，形成无限治疗循环。现已改为查询统一状态服务的真实驱散结果：一阶段按普通驱散、二三阶段按强驱散判断；【镜界标记】规则本身保持不可驱散。
- 普瑞赛斯护盾曾依赖日志中是否包含某段中文来决定事件顺序。现在使用显式减伤通道，不再让台词控制规则。
- 源石结晶承受屑的伤害转移时，矿石病副作用曾先于具体伤害结果。现在沿用当前伤害运行时与同一根事件链。
- 并行压测父进程曾在子进程 `exit` 时过早读取尚未写完的报告。现等待 `close` 后合并，避免假失败或缺报告。

### 统一架构检查

- 生产代码中只有统一状态服务可以直接增删 `fighter.statuses`。
- 旧 `statusLifecycle`、`statusRules` 和 `timedStatModifiers` 已删除，不存在双重计时或双重属性修正入口。
- 通用机械通过目录和 `queryMechanic` 查询；角色身份通过 `hasIdentity` 查询。
- 每个状态时钟只由统一推进器前进一次；新施加或刚刷新的状态不会在同一时点立即损失回合。
- 阶段、锁血、复活、场外 OB、装备、召唤归属、专属标记和 NPC 阶段仍保持独立机制，没有被误塞进通用状态生命周期。

## 日志因果审查

人工逐条审阅发现并修复了以下跨系统问题：

- 完整法术抵挡发生在技能正文前时，战报只剩“抵挡”，看不到谁发动了什么。现先记录结构化【技能发动】，再记录抵挡结果。
- 鸮【胜兵/哀兵】倍率已参与最终伤害，但过去不说明调整前后数值；在屑转移、沫沫分摊或柚子护盾链中还会晚于结果出现。现在攻击方倍率说明在进入目标侧结算前写出。
- 牢鳄抽到沉船是单体攻击，却写成“对周围”且遗漏目标；现明确冲向具体目标。
- 封印锁链、白龙威压、融合不稳定和神威压制属于永久面板改写，旧文案看起来像漏登记的临时状态。现明确标注“永久”及精确比例。
- 【灰流丽】曾先写眩晕、后写卡牌发动。现在先写发动原因，再施加状态并记录成功或失败。
- 【神不死鸟】攻击普瑞赛斯时曾先断言零伤害、后写护盾吸收。现在先刷新减伤/护盾事件，再给目标结果摘要。
- 矿石病致死时，复活、反击和死亡曾插到【矿石病侵蚀】总起因之前。现在先写侵蚀起因与真实损失，再依次结算保命、复活、反应和死亡。
- 沫沫 `|OMO` 分摊后的失败日志曾误称屑的随机恶作剧。现按屑、阿喃那、帝王之征和 `|OMO` 分别显示真实分流原因。
- 扩音处刑击碎源石系 NPC 曾使用“大脑宕机”等生物死亡文案。现改为源石结构专属描述。
- 玄凝有存活队友时也会把普通残局写成【1vX残局】。现在有队友显示【残局专注】，真正单人时才显示【1vX残局】，不改变数值或选择概率。
- 召唤物锁血、献祭、融合、高级召唤、反击、强驱散击飞落地、场外 OB 回归、表情认主和柚子连续阶段边均已核对完整前因后果。

完整逐行记录位于 `.tmp/namearena-deep-audit/manual-review-progress.md`，原始战报位于 `.tmp/namearena-deep-audit/manual-logs/`。

## 界面审查

### 桌面端

- 2、9、14、18 人在 1440×900 与 1920×1080 下均无卡片重叠、越界或文字遮挡。
- 15–18 人使用固定宽度密集布局；19 人以上才进入拥挤布局，避免人数刚超过 9/10 就全部挤到顶端。
- 中文角色名保持完整可读；过长技术名 `M1A2_abrams_sep` 在极密布局中按容器省略，不挤压生命条或相邻卡片。
- 伤害数字和全屏动画标题使用桌面、紧凑、低高度横屏和手机固定字级，不再随 `vw` 连续放大或缩小。
- 玩家手动查看角色详情后，下一次行动会恢复跟随当前行动者；点击空白舞台也可立即清除手动焦点。

### 移动端

- 844×390、932×430 横屏和 390×844 竖屏均检查了舞台、战报切换、状态详情、播放倍率、经典/新舞台切换和页面溢出。
- 18 人横屏以及召唤物加入后的 20 单位竖屏均为零卡片重叠、零横向越界；页面自身没有产生横向或纵向溢出。
- 横屏低高度下收紧了工具栏但保留稳定尺寸；选中态不再通过放大导致边缘被裁约 5px。
- 横屏模式切换不再把“新舞台”截成含义不清的单字“新”，紧凑模式明确显示“新版”。
- 竖屏操作按钮保持至少 32px，更新入口为 44×44 图标按钮，不遮挡角色详情。
- 移动端切到战报时舞台特效会暂停，切回舞台后恢复；不会在不可见页继续积累待播动画。

### PWA 更新提示

- 桌面端移到顶部居中，不再挡住名字大乱斗模式和倍率按钮。
- 横屏低高度使用右下紧凑条，竖屏窄屏使用带无障碍名称的图标按钮。
- 外层不拦截舞台点击，只有更新按钮自身接收指针事件。

最终截图位于 `.tmp/namearena-deep-audit/ui/`。

## 性能审查

- Tailwind v4 原本仍会默认扫描整个仓库，会从历史 ZIP、视频和大体积审计产物的二进制字节中误识别畸形工具类，造成开发热更新 PostCSS 解析失败。现已使用 `source(none)` 关闭默认扫描，只保留显式声明的源码来源；重启开发服务后页面与热更新恢复正常。
- 在生产构建页面连续执行 5 次“重置→重新开局→播放”：每轮稳定为 554 个 DOM 节点、2 个 Canvas、14 张战场卡片、0 个遗留对话框/全屏动画/弹窗。
- 每次重置后稳定回到 238 个 DOM 节点、1 个全局 Canvas、0 张战场卡片；没有出现单调累积，因此没有发现组件级 DOM、Canvas 或视觉容器泄漏。
- 视觉队列在隐藏、换局和卸载时会取消 RAF、计时器、延迟后续和角色位移，避免多局后动作越来越多。
- 审查期间发现一个此前遗留约 6 小时的压力测试进程持续占用 CPU，已终止。它是开发环境明显卡顿的重要来源之一，不是网页空闲时自行产生的进程。
- 受控浏览器不暴露 JS heap 采样和 `document.getAnimations()`，因此无法把“保留内存增长低于 20%”量化到 JS 堆字节；本轮以五轮稳定 DOM/Canvas/视觉对象数量作为可验证替代证据，并如实保留这项工具限制。
- 截图已经确认舞台 Canvas 与战斗特效非空；受控浏览器不允许直接读取 Canvas alpha 像素，因此没有伪造精确像素计数。

## 自动化验证

| 验证 | 规模 | 结果 |
| --- | ---: | --- |
| 完整回归 | 301 场 | 全部通过；运行、状态和日志错误均为 0 |
| 机制矩阵 | 248 场 | `mechanismOk=true`；248 场全部结束，超时/运行/不变量/日志错误均为 0 |
| 视觉事件终验（净化修复前） | 1,000 场 / 4,492,817 事件 | 结构错误 0；当时 7 场达到审计回合上限 |
| 因果链审计 | 100 场 / 431,863 事件 | 根事件、行动配对、伤害、状态、死亡顺序错误 0 |
| 2v2 | 2,000 场 | 硬错误 0，超时 0；最低队友/敌人覆盖 28/70 |
| 3v3 | 2,000 场 | 硬错误 0，超时 0；最低队友/敌人覆盖 111/172 |
| 4v4 | 2,000 场 | 硬错误 0，超时 0；最低队友/敌人覆盖 225/319 |
| 5v5 | 2,000 场 | 硬错误 0，超时 0；最低队友/敌人覆盖 394/511 |
| 强制普瑞赛斯（净化修复前） | 500 场 | 硬错误 0；499 正常结束，1 场达到审计回合上限 |
| 含水 FFA（净化修复前） | 3,000 场 | 硬错误 0；2,989 正常结束，11 场达到审计回合上限 |
| 无水 FFA 最终复跑 | 15,000 场 | `mechanismOk=true`；超时/运行/不变量/日志错误均为 0；8 场正常同归于尽 |
| 刺猬人旧超时种子复跑 | 33 场，单场上限 10,000 回合 | 33 场全部正常结束；超时/运行/不变量/日志错误均为 0 |
| 人工完整战报 | 20 份 / 35,858 条编号事件 | 已逐条审阅；确认问题均已修复并补定向回归 |
| 静态检查 | TypeScript、完整 ESLint、Next 16.2.9 Turbopack 生产构建、架构扫描 | 全部通过 |

11,500 场分层覆盖累计执行 3,001,955 个全局行动、生成 17,383,199 条日志；运行错误、状态不变量错误、自动日志错误均为 0。

### 最终无水 FFA 统计

| 角色 | 胜利积分率 |
| --- | ---: |
| 克蕾儿丝菲尔 | 10.180% |
| M1A2_abrams_sep | 9.893% |
| 兔卷卷 | 9.287% |
| 表情 | 8.873% |
| 刺猬人 | 8.090% |
| 萌月沫沫 | 7.780% |
| 玄凝 | 7.247% |
| 丝瓜uli | 7.200% |
| 小汀 | 6.843% |
| 屑 | 6.747% |
| 牢鳄 | 6.723% |
| 鸮 | 6.177% |
| 柚子 | 4.907% |

- 15,000 场使用与修复前完全相同的种子基准 `1910000`。修复前 `timedOut=109`，修复后 `timedOut=0`；刺猬人胜利积分率由 5.367% 恢复到 8.090%。本轮只纠正错误决策，没有调整角色数值。
- `drawOrUnknown=8`：5 场由普瑞赛斯事件消灭最后玩家，2 场最后幸存者死于灼烧，1 场克蕾儿处刑小汀时被反伤同归于尽；8 场均有完整原因、死亡和结算日志。
- 普瑞赛斯出现 4,301 场（28.673%），其中 4,261 场进入二阶段（登场局的 99.070%），2,658 场被击退（登场局的 61.800%）。

## 非 Bug 与待策划决定

- `克蕾儿丝菲尔+表情` 与少数高回复/高级召唤组合仍可能形成高续航局；本轮 15,000 场没有任何一局达到 1,600 行动上限。
- 鸮【过江】的定义是“协同攻击同一受害者”。当被标记者攻击鸮自己的召唤物时，鸮会按现有定义协同攻击该召唤物；这是已知设计取舍，不擅自改规则。
- 不会行动的源石结晶仍可承受虚弱等行动类状态；状态会正常计时并自然结束，但对其没有实际行动效果。它不破坏结算，是否改成免疫属于策划语义决定。
- 普瑞赛斯护盾归零后按专属规则退场，不把溢出伤害继续灌入本体；已有“有结晶保留 1 点护盾”和“无结晶破盾退场”两类回归，这是既定规则。
- 同一 AOE 里，帝王之征可能既替鸮承伤，又因自身也是可选单位而直接受击；两次伤害拥有不同目标路径和日志原因，并非重复结算。
- 普瑞赛斯登场局的击退率为 57.090%，略高于此前希望的 50–55%。这是平衡观察，不属于运行或结算 Bug，本轮按约定没有调整数值。

## 本轮边界

- 没有根据本轮胜率统计修改角色数值。
- 没有进行数据库迁移或写入网站数据库。
- 没有推送 GitHub，没有部署服务器。
- 审查产物和截图保留在本地 `.tmp/namearena-deep-audit/`，用于继续复核和复现固定种子。
